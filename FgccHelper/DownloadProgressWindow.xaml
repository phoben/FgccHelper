<Window x:Class="FgccHelper.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="正在下载更新" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Icon="icon.ico"
        Topmost="True">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                  Text="正在下载更新包..." 
                  FontSize="16" 
                  FontWeight="Bold"
                  Margin="0,0,0,20"
                  HorizontalAlignment="Center"/>
        
        <!-- 进度条 -->
        <ProgressBar Grid.Row="1" 
                    x:Name="DownloadProgressBar"
                    Height="25" 
                    Margin="0,0,0,15"
                    Minimum="0" 
                    Maximum="100" 
                    Value="0"/>
        
        <!-- 进度信息 -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="下载进度:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="ProgressText" Text="0%"/>
                <TextBlock x:Name="FileSizeText" Text="0 KB / 0 KB" Margin="0,5,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1">
                <TextBlock Text="下载速度:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="SpeedText" Text="0 KB/s"/>
                <TextBlock x:Name="TimeRemainingText" Text="剩余时间: 计算中..." Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
        
        <!-- 状态信息 -->
        <TextBlock Grid.Row="3" 
                  x:Name="StatusText"
                  Text="正在连接到服务器..."
                  TextWrapping="Wrap"
                  HorizontalAlignment="Center"
                  Margin="0,0,0,20"/>
        
        <!-- 按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" 
                   Content="取消下载" 
                   Click="CancelButton_Click"
                   IsEnabled="True"/>
        </StackPanel>
    </Grid>
</Window>