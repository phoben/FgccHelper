我将执行以下步骤来完成您的需求：

1.  **硬编码配置**: 修改 `FgccHelper/Models/UpdateConfig.cs`，将 `CosBucketUrl`, `CosSecretId`, `CosSecretKey`, `AutoCheckUpdate`, `CheckIntervalHours` 修改为只读属性，并直接在代码中赋予固定的值。仅保留 `CurrentVersion` 为可读写属性，以便更新时记录版本号。
2.  **调整配置加载/保存逻辑**: 修改 `FgccHelper/Services/UpdateManager.cs`。
    *   `LoadUpdateConfig`: 继续从文件加载，但由于配置项已变为只读，文件中的旧配置（如果存在）将被忽略，只读取 `CurrentVersion`。
    *   `SaveUpdateConfig`: 仅保存包含 `CurrentVersion` 的配置对象。
3.  **移除更新设置界面**:
    *   删除 `FgccHelper/UpdateSettingsWindow.xaml` 和 `FgccHelper/UpdateSettingsWindow.xaml.cs` 文件。
    *   修改 `FgccHelper/App.xaml.cs`，移除 `ShowUpdateSettings` 方法，并简化 `CheckForUpdatesAsync` 方法（移除对配置为空的检查，因为现在是硬编码的）。
4.  **移除菜单入口**:
    *   修改 `FgccHelper/MainWindow.xaml`，删除“更新设置”菜单项。
    *   修改 `FgccHelper/MainWindow.xaml.cs`，删除对应的 `MenuUpdateSettings_Click` 事件处理代码。

这样修改后，用户将无法更改更新配置，系统会强制使用内置的 COS 配置，并且保留了自动检查更新和帮助菜单中的手动检测更新功能。